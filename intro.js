/*JavaScript is a versatile programming language created by Brendan Eich in 1995. 
Originally built to add simple interactivity to web pages inside the Netscape browser, 
it has grown into a core web technology alongside HTML and CSS.

Where it runs:

Browser (Client-side): Handles DOM manipulation, form validation, and user interactivity.

Node.js (Server-side): Runs backend applications, REST APIs, and file system operations.

Cross-platform: Used for mobile apps (React Native), desktop software (Electron), and IoT applications.

The Web Development Analogy:

HTML: The structure (skeleton/body of a car).

CSS: The style and layout (paint, upholstery, styling).

JavaScript: The behavior and engine (the motor, pedals, steering control).*/

// 01-intro.js
console.log("JavaScript is running!");
